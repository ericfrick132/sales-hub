services:
  saleshub-api:
    build:
      context: ./backend
      args:
        PROJECT: SalesHub.Api
    image: sales-hub:latest
    container_name: saleshub-api
    restart: unless-stopped
    env_file:
      - .env
    environment:
      ASPNETCORE_URLS: http://+:8080
      SALESHUB_RUN_WORKERS: "true"
      SALESHUB_AUTO_MIGRATE: "true"
      TZ: America/Argentina/Buenos_Aires
    volumes:
      - /opt/saleshub/public/downloads:/app/wwwroot/downloads
    networks:
      - caddy
      - evolution
    # Permite que el container alcance un proxy corriendo en el HOST (ej. el túnel
    # SOCKS5 reverso de la compu de Eric, bindeado en 0.0.0.0:1080 del droplet).
    # Así Instagram__ProxyUrl puede ser socks5://host.docker.internal:1080 sin
    # hardcodear la IP del gateway de docker (que cambia si se recrea la red).
    extra_hosts:
      - "host.docker.internal:host-gateway"
    # Los workers de IG corren acá y lanzan Chromium (Playwright), que solo ya usa
    # ~300-500MB por instancia. Con 450m el contenedor se OOM-killeaba al abrir el
    # navegador. El host tiene RAM de sobra (~2GB libres + swap), así que damos
    # headroom para 2-3 navegadores en simultáneo.
    mem_limit: 1500m
    mem_reservation: 400m
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8080/health || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 60s

  # Lector de chats de WhatsApp (Node + Baileys nuevo). No sale a internet por Caddy: sólo lo
  # llama la API por la red interna. Existe porque el protocolo de WhatsApp Web sólo está en
  # Node y la Evolution de producción empaqueta una baileys que descarta el número real de los
  # chats (LID); esto corre la versión que sí lo trae, sin tocar el tráfico de las 6 apps.
  wa-reader:
    build:
      context: ./services/wa-reader
    image: wa-reader:latest
    container_name: wa-reader
    restart: unless-stopped
    env_file:
      - .env
    environment:
      API_URL: http://saleshub-api:8080
      TZ: America/Argentina/Buenos_Aires
    volumes:
      - wa-reader-data:/data
    networks:
      - evolution
    mem_limit: 500m
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8090/health || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s

volumes:
  wa-reader-data:

networks:
  caddy:
    external: true
    name: n8n-docker-caddy_default
  evolution:
    external: true
    name: evolution-api_evolution-network
